# 三模型同期计时：独立候选代码审查

**结论：候选代码与6435条计时合同一致，未发现需要阻断的实现问题。当前没有计时执行授权，也没有实测结果；完整计时分支仍待全60数值审阅通过、环境安静和根任务另行授权。**

候选`benchmark_attention_extension.py` SHA：`a98585be1e4697f4d246cdbc64963270552d6470febcd65ea22b99377fe5395c`。计时计划SHA：`128fa4a6f448937dbd688bec69fd651bcd4d451a1ead426fb58bf3f2d14cd71a`。只读复用的原助手源码SHA：`aad7de1e3e332a119adf5ea97f2813bf47dd8d3fe4d136a4ba60cb096f96bcf9`；原七模型计时驱动没有运行或被修改。

- 选择固定为IID首种子42、全部160个冻结测试ID、匹配集合/sum GNN/注意力三模型；三个模型在同一新测量批次中比较，每轮模型顺序旋转，各位置恰一次。CPU/CUDA顺序在三轮交替，CPU四线程。原七模型14355条记录被单独绑定且不覆盖、不合并，也没有从新旧时段择优的代码路径。
- 模型测量条数为3轮×2设备×3模型×2接口×(160个单例+5个32例批次)=5940；共同CPU特征准备为3×(160+5)=495；合计6435。总体分组为2×3×2×2+2=26，逐轮为78。每条案例是完整61点响应，单位和吞吐量分母正确。
- 驻留接口预先整理并转移共同输入，只测模型调用至设备响应。公共接口实际重建五张量、collate/传输、模型调用和CPU NumPy返回；未把特征重建或传输排除后仍称端到端。所有模型付出相同的共同特征构造成本，包括自身未使用的属性，限制说明正确。权重加载、导入、磁盘/JSON和FE成本均在计时外。
- 每轮/设备/模型/批次/接口均先完整160例预热，再额外十批次；CUDA在每个计时起点前和调用结束后同步。形状、有限性检查和CSV写入在计时外。float32、int64索引、matmul TF32关闭、cuDNN TF32开启和确定性配置与冻结执行一致。注意力的padding和Python打包未做速度导向修改，均包含在其实际模型调用中。
- 完整60、真实回放聚合、独立数值结果通过、实际新60/旧120来源、当前无检测到的项目Python计算和独立计时专用授权共同控制执行。首次执行前做全文件身份检查；后续各轮/模型复查队列、最终证据/源、选中权重和并发进程，没有在每次测量之间重新扫描全部180个权重。系统中的匿名或非Python负载不能由该检测证明不存在，代码和计划已说明。
- 输出仅写新目录，已有`measured`尝试拒绝覆盖；失败保存部分记录与failure，不给完整结果状态。总结保留原始秒数、p50/p95、总案例数/总时间及批次摊销；不能把批次摊销值叫串行单例延迟，也不能称新推理批次为隔离训练成本。

中断前实际独立拒绝执行检查已成功保存：`independent_deferred_gate_probe_evidence.json`，2026-09-07 10:17 UTC，33/60时直接调用真实`run`接口，结果返回2；成绩/数组/权重/数据/模型构造/实际计时钟/完整载荷核验的禁止探针全部零，未生成测量目录，原七模型summary SHA保持不变。这是未完成门槛的实际证据，不是完整计时通过，也不是对全部潜在操作系统负载的证明。本次恢复确认该文件已落盘，没有重复探测或启动测量。

全60实际数值检查之后，根任务需另写绑定plan、完整report/run_integrity及独立数值review的计时授权。完成实测后仍须独立核对6435条覆盖/顺序/单位/算术与图表。科学结论和论文送审状态不由本软件审查决定。
