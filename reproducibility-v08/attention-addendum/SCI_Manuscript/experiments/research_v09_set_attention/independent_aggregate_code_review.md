# 集合注意力扩展聚合器：限定代码审查

结论：**当前候选代码未发现阻断性的统计定义或API问题，可等待全部60次完成后执行真实审计。** 这不是完整分支已运行、结果审查通过或整稿送审通过。

审查对象为 `aggregate_extension.py`，SHA `aa06a3280fed95be571f1b39677c84c20d193804897d4f4d55ec7a99053a2e7a`。本次只读聚合器、所调用API实现、冻结计划及缺矩阵门槛证据；没有读取部分新模型预测/成绩，没有运行聚合、训练或推理。

1. **全矩阵门槛。** `readiness`先核验冻结源/计划、60个注册身份的文件存在性、元数据、状态及完整队列，意外结果路径失败。仅全部60且无活动单元才进入载荷；已有真实门槛证据为1/60、`WAITING_FOR_ALL60_COMPLETE`、0成绩载荷/0预测数组。缺矩阵状态不会写完整报告。
2. **来源和回放。** 新60逐一绑定模型结构/参数、源/计划/数据/划分、授权、日志、最早验证最小值、停止规则、学习率与检查点；保存预测用共同有序ID/真值/时间对账。原120比较运行来自固定420审计及实际保存文件SHA，未重新拟合。原CPU最大差`<3e-6`与超限后原GPU逐位相同两个分支保持原规则，没有放宽容差。
3. **可调用接口。** 新模型兼容原 `rr.predict` 的六张量签名、`model.eval()`与推理上下文；原 `computational_replay`不依赖原模型类，能够接受新模型。工程函数的返回值和主要统计字段与此聚合器一致；使用显式float64误差，逐例、逐时间和分层数量不混用。
4. **配对统计。** 两项固定主要差值为匹配集合−注意力、sum GNN−注意力，正值有利注意力。主要5000次使用同一几何及五种子配对重抽样；工程1000次各模型固定相同随机种子、ID顺序和种子顺序，共同重抽样差值直接计算，未相减区间端点。五种子均值不等于五倍独立几何。原不适用于此扩展的旧门槛字段被移除，探索性状态保留。
5. **NA和条件事件。** 工程点对比要求全部五种子有定义；共同有效重抽样数和比例明确导出。各模型事件时间仅在各自参考/预测共同越界群组上定义，文本已明确不是两个模型共同事件交集。零事件和常数排序保持NA，未置零。Smoldering的恒值/非恒值及原保留/恢复状态均有固定分层输出。
6. **输出。** 新目录拒绝覆盖旧快照；先核验全部新检查点，再写分协议统计。失败会留`failure.json`，完整报告只能在所有步骤成功后写入。输出与分析代码SHA、原120审计和新60审计单独绑定，不改原420结果。未混入原七模型预算分析，也未创建第八模型的既定计时成绩。

完整60分支、实际新模型CPU/GPU回放、各协议数值复算、工程删失覆盖及生成后图表仍须完成后独立核查。代码审查不能预先清除未来数值/回放失败，且该模型是在原结果审阅后提出，所有后续比较必须持续标识为探索性扩展。
