\paragraph{Exploratory three-model comparison.}
Equal-case mean absolute error (MAE) for the capacity-matched independent-node set model, sum GNN and set-attention control across all 12 frozen protocols. Small points denote five seeds, large points their mean, and whiskers one sample standard deviation. The independent geometry count is $n$, not $5n$. Each case contains a complete 61-sample archived response-index trajectory. All panels use the same linear scale, with values in $10^{-3}$ units. The exploratory extension adds 60 attention runs and reuses 120 comparator runs without reselection; it supplements the original 420-run comparison.

\paragraph{Exploratory paired representation effects.}
Matched-set MAE minus attention MAE and sum-GNN MAE minus attention MAE. Positive values favor attention. Panels (a,b) enlarge the prespecified crack-count transfer question, training on 1--8 cracks and testing on 9--15 cracks ($n=264$); panels (c,d) retain every protocol and complete interval on a common full linear scale. The expanded and full axes have different, explicitly labelled ranges. Points average paired case errors over five seeds; whiskers are percentile 95\% intervals from 5,000 crossed geometry--seed bootstrap replicates with intact time histories. These approximate intervals are neither predictive uncertainty intervals nor causal estimates of interaction mechanisms. All positive and negative results are retained. The attention model was specified after review of the original seven-model results, so these comparisons remain exploratory.
