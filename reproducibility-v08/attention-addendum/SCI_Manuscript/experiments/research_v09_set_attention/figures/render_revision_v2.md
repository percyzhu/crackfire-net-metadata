# 实际渲染修订v2

完整60汇总与独立结果复算通过后，首次真实调用原候选d667d466的绘图分支触发两项显示问题：Arial缺少Unicode上标负号字形；Matplotlib自动刻度生成了已定轴范围之外的刻度，画布文本检查因`60`刻度越界而终止。失败目录`complete_reviewed_v1`及failure.json完整保留，没有覆盖或删除。

新文件`generate_extension_figures_v2.py`只把单位标签改为mathtext，并显式限制绘制的刻度位于已有线性坐标范围内。图的原始数值、全部12协议、三模型、五种子、均值/SD、配对效应与CI、轴上下限、颜色、两图/两表结构及完整审阅门槛均不变。没有改训练、聚合、独立结果审查或容差。

新调用须输出到`complete_reviewed_v2`，最终仍需真实视觉与LaTeX检查。本修订不掩盖首次失败，也不代表新的科学实验或重新计算了统计结果。
