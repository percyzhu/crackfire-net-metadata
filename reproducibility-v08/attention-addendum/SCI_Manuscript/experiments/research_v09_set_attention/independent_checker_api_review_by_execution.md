# 全矩阵独立检查器的有界源/API复核

2026-09-07，执行代理只读复核`audit_complete_extension_matrix.py`，最终候选SHA256为`6757565747f31bf30f599aff2f2fe7c12b3762d9bedfbcf225783e8fa5f24165`。本复核者编写了原聚合器，因此这里是另一实现的接口、算法与门槛检查，不是最终科学结果的再次独立复算。

核对当前真实聚合器、原统计源、工程指标源及既有软件证据的API：新60/复用120身份、原始有序ID与61时刻、原global(1,3)/node(N,11)/time(61,4)张量、最早验证最小值/早停/余弦日程、CPU严格3e-6和原GPU exact回放状态、几何排序及种子权重、5000次主要配对bootstrap、四项final/J的1000次共同重抽样、事件/删失/NA/排序定义均相符。检查器未导入学习模型，也未调用聚合器的统计函数。

首次复核建议的三项补强已在最终候选中核实：存在aggregate/failure.json即拒绝；所有未注册result路径拒绝；48次要CI分别核对1000重抽样、有效比例、有限有序端点以及ESTIMATED/NA状态。没有未解决的阻断性接口问题。

本次未读取新实验部分成绩、预测或权重，未执行完整结果检查器。完整分支仍必须在全部60完成及真实汇总之后运行；软件复核不能提前赋予数值结果、图表或论文通过状态。其余次要指标的区间按检查器披露范围核对，不能声称逐一独立重抽样了全部分类/排序/条件时间CI。
