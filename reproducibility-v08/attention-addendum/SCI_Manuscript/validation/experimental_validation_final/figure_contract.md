# Experimental comparison figure contract

The figure demonstrates that the archived20mm thermal-model comparison tracks observed heating at three interior depth groups, with time-weighted RMS differences35.5/43.3/40.0 degrees C against the arithmetic mean of two supplied experimental groups. It is a recovery and transparent redraw of the existing validation, not a new FE solve or a new sensor-matched experiment.20mm is fixed by the original validation subsection, before extraction; all14 source mesh curves and nine workbook Abaqus curves are retained in CSV without selecting the best result.

Four panels: (a) a restrained specimen/exposure schematic based on the supplied experimental description, with actual600mm specimen length,140x200mm section, four longitudinal heated faces and protected ends; (b-d)15/30/40mm depth-labelled temperature histories with both experimental groups, their arithmetic mean and the recovered20mm FE curve. The schematic does not invent thermocouple coordinates. Time-weighted RMS uses the stated0-3600s common grid. The two-group spread is not a confidence interval or a verified independent-specimen variance.

Final183x152mm; Arial minimum8pt; PDF/SVG vector plus PNG preview. Use the established Python/matplotlib workflow exclusively. Retain true curves, early values, native experimental data and all extraction metadata. No AI-generated experiment image, shifted initial temperature, inferred TC assignment or clipped predictive evidence. Source PDF polyline recovery is checked against all nine independently supplied observation curves; this gives an actual plotting-reconstruction diagnostic, not a universal error bound on recovered FE output.
