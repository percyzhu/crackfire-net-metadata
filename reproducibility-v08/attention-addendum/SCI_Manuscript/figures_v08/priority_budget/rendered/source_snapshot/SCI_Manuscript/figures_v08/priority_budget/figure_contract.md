# Fixed exploratory budget-priority figure

The main figure uses the root-selected models, in order: fire only, capacity-matched DeepSets, sum GNN. Each panel contains all12 fixed protocols in protocol order and six columns: terminal response at10%,25%,50% nominal slot budgets, then normalized time average J at the same budgets. Every cell is the five-seed arithmetic mean of L, multiplied by100. All216 main cells are retained, including negative or undefined entries. A common color scale covers every actual raw value; undefined ratios use gray cells markedNA. Cell labels show at most one decimal place; redundant .0 is omitted to improve adjacent-cell spacing. A rounded negative zero is printed as0 while its raw value is preserved. The manuscript denotes the source metric L by eta_b; the figure colorbar uses its descriptive name to avoid notation mismatch.

L=(U_random-U_model)/(U_random-U_oracle), where U is the expected selected-reference mean under the fixed exact-cutoff-tie rule. L is the fraction of available oracle improvement, not reduction in failure risk, computing cost or physical-capacity loss. No confidence interval is invented. All7 models' seed-level and summary U, raw/display regret, G and L, plus all72 reference budgets and fixed paired contrasts, accompany the figure. The plot makes no pooled score over overlapping protocols.

The source gate requires completed independent budget arithmetic and the independent code/semantic review, exact protocol/analysis/generator identities, and hashes for every original output before reading figure values. The figure does not import or execute the budget generator. A separate figure checker recomputes all504 summary cells from2520 seed records, all216 plotted values and their complete coverage; it checks units, undefined status, axis inclusion, fonts and PDF vector export. Actual visual review remains a separate final step.

Final size183×150mm, minimum8pt, pure-vector PDF/SVG with PNG preview. The fixed full12×6 coverage and three chosen representations precede plotting and cannot be changed according to the results. This is explicitly post-review exploratory analysis of the archived numerical response. The original420 records and sealed reproducibility package remain unchanged.
