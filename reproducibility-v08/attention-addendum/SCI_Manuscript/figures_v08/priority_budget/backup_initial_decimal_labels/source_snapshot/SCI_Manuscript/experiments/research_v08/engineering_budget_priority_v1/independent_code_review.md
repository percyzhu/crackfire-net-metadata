# Independent code and statistical-meaning review

Reviewer: `/root/engineering_validation_v08`. Review date: 7 September 2026. This is a bounded read-only review of the approved protocol, `budget_priority.py`, actual input-equivalence gate and completed analysis metadata. No generator, model, optimizer or FE calculation was executed by this review. The separate independent arithmetic audit remains the authority for checking every output number.

**Decision: no blocking implementation or statistical-definition issue found for this fixed post-review exploratory analysis.**

1. **Budget and expected selection.** Exact integer arithmetic computes floor(N/10), floor(N/4), floor(N/2), without floating-point budget rounding. The cutoff uses the kth order statistic; strictly lower scores receive 1, exact cutoff ties receive r/e, and larger scores receive 0. The assertion checks the budget sum and probability range. This is the expected result of sampling exactly r boundary cases, not a fractional physical experiment or a selected favorable tie resolution. No reference score breaks a prediction tie.
2. **Reference comparisons.** Oracle U is the average of the k smallest reference endpoints. Uniform-without-replacement expected U is the full-population mean; its SD uses the finite-population correction and sample variance. This SD is a policy-distribution SD, not a confidence interval for model advantage. Both endpoints apply the same terminal or normalized trapezoidal operation to float64 copies of reference and original predictions. Neither clipping nor monotonic correction is introduced.
3. **Regret, gain and undefined ratios.** Raw regret is retained, while negative rounding error can only reach the display-level zero after the1e-12 assertion. Materially negative regret fails. Negative gain and negative oracle-improvement fraction are retained. Available improvement≤1e-12 yields an undefined fraction, leaving U/regret/gain defined. Because availability is reference-only, all models and all five seeds share its defined/undefined status for the same protocol, endpoint and budget.
4. **Seed summaries and contrasts.** Every group asserts seeds42–46 exactly; sample SD uses ddof1. Reference N is not multiplied by seeds. Both fixed paired contrasts use the same candidate population, seed, endpoint and budget. Their sign agrees with the stated second-model-favored convention, and their equality to regret differences is checked. The result metadata retains all12×7×5×2×3=2520 utility rows and no cross-protocol pooled utility. The99470 endpoint rows and298410 weights agree with1421 overlapping test occurrences,7 models,5 seeds,2 endpoints and3 budgets.
5. **Input-equivalence treatment.** Source inspection of `models.py`, `capacity_matched_set.py` and the mean-message wrapper confirms that the five node models use node features, global features and temporal inputs. The verified complete graph has all ordered distinct-node pairs. A canonical node-feature multiset plus global/time inputs is a conservative necessary component of full attributed-input equivalence: ignoring edge attributes can produce extra suspected duplicates, but cannot conceal two identical attributed graph inputs. The observed absence of duplicate canonical node/global/time inputs therefore justifies leaving these five models' raw scores unchanged under the approved rule. The check does not establish uniqueness of learned latent vectors or output scores. Fire-only and global-statistics normalization uses only effective inputs and raw predictions, never references; the latter is actually an identity operation for every tested population.
6. **Provenance and scope.** The execution binds the approved protocol hashes, complete timing, final420 independent reviews, prediction/checkpoint identities, split order, reference and61-point grid. It writes to a new directory with exclusive creation, preserving original predictions and major analyses. The status explicitly says post-review exploratory; prior accuracy and engineering results were known. This does not create a preregistered confirmatory claim.

Two interpretation requirements remain for manuscript/figure use:

- `reversed_node_order_invariance_verified` in the input gate refers to invariance of the **canonicalization function** under reversed node rows. It is not997 actual model-forward permutation tests. The existing gate explanatory text already limits the claim; no frozen result edit is required.
- Scores tied mathematically after input normalization can yield roundoff-scale gains around zero in floating arithmetic. The implementation allows only1e-12 in its constant-score consistency assertion. Such negligible residuals are not evidence of geometry information; preserve raw values and display with appropriate precision. Model selection ranges are seed variation, not geometric uncertainty intervals.

The original protocol and generated results were not modified. Full arithmetic approval, actual budget figure readability and final manuscript review are separate checks.
