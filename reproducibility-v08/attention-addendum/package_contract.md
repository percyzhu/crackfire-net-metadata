# Attention60 and original-seven budget addendum

This is a separate, local author package. The sealed `EAAI_reproducibility_v08_complete` base and its manifest SHA256 `b188a4b5a3907e1375ac77109aa5b72a17f697c2b19ecf5c975777e6a288082c` remain unchanged. No original420 weight, original420 prediction, learning tensor or large FE field is copied into the addendum. The default base location is its sibling `../EAAI_reproducibility_v08_complete`; an explicit relative base override is resolved against the addendum location, never the caller's working directory.

The payload contains all60 additional attention checkpoints and their six required run artifacts, all12 completed three-model statistical/engineering summaries, the independent full-matrix review, the additional frozen source/plan/authorization/software checks, the complete original-seven budget results and code/arithmetic/input-equivalence evidence, and the actual budget figure sources, data and QA. Additional attention figure/timing companions available and complete at build time may be separately inventoried. This does not claim that every historical generator has been ported. All original plans and historical absolute paths retain their original bytes and SHA; a separate relocation map specifies actual package/base locations.

The packager must stop before reading performance outputs if the unique complete60 aggregate report or independent full-matrix review is missing. Seal requires the review's actual PASS status, true numerical_review_passed, exact60/120 coverage, report/integrity/plan hash matches, all12 COMPLETE protocol/engineering files, all60 metadata and source artifact identities, and completed independent budget arithmetic. No test-based file selection is allowed. The builder never overwrites any destination; partial copies remain in a separately named staging folder after an error.

Portable verification checks both package identities, all copied file hashes, all referenced base-file hashes, exact coverage and saved attention prediction identities/metrics. A fixed, preselected `iid997 / set_attention / seed42` CPU command replays all160 test cases and61 times under the existing strict maximum difference <3e-6 criterion. No optimizer update, CUDA replay, model selection, all60 model re-inference or rebootstrap is implied. CPU tolerance must not be widened to obtain a pass. If this fixed replay differs, report the difference honestly.

The isolated timing window has ended. The independently reviewed 6,435-record three-model timing session and the archived experimental-comparison recovery are required companions for this build. Thermal companions include only the small original workbook/vector figure/plotting source/description, recovered data and final figures/audits, without large FE fields or the complete thesis. An already-existing verified relocated original420 base may be reused for actual cross-directory verification, avoiding another425MB copy. Reports are written outside the sealed payload with exclusive creation, and explicitly bind the actual copied location and working directory. No public upload, repository creation, license choice, submission or manuscript-acceptance claim is made.
