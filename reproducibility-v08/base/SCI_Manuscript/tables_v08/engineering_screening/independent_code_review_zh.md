# 工程补充表生成器：有界独立开发审阅

2026-09-07。**代码、门槛与已完成协议接口核查通过；完整结果表尚未生成或编译。** 本次审阅不替代真实420次数据分支核查、生成后数字复核或投稿审阅。

审查版本：`generate_engineering_tables.py` SHA256 `d8ef80e36761cb77f387a7d8e98866f34a29986eed109913d070acbd20d767b8`。机器记录为同目录 `independent_code_review.json`，绑定生成器、contract、caption、README与两个接口来源CI的SHA。

实际请求生成时，完整性门槛因尚未满足12协议/420次、全部CI及独立审阅要求而停止；成绩载荷读取为0，未创建`generated/`。代码按当前来源格式，在所有文件与完成状态齐备后才检查CI和审阅元数据头，随后进行源身份绑定。最终全部420次的身份绑定分支仍未实际执行，不能标为已验证。

统计定义没有发现未解决的实质错误：

- 终值/J差为容量匹配集合MAE减求和GNN MAE，正值有利于求和GNN。原尺度CSV不缩放；LaTeX数值乘1000并注明单位为10^-3响应单位，不是百分数。CI直接引用既有1000次共同重采样的差值区间，不相减模型区间端点，不依据舍入后的符号判优。
- J是轨迹梯形积分除以3600秒所得时间平均响应，再计算逐例标量误差；它不是逐时间点MAE的平均，也不是共同事件计数。每种子先按案例平均，再平均五种子；样本SD与CI区分明确。
- 第二表分别取五种子的最大FN、最大FP及最小参考—该模型共同事件数/E。极值可来自不同种子，不组成一个最差检查点；共同事件也不是两个模型的交集。N仍是独立测试几何数，不是5N，不跨重叠协议相加。
- 0:60:3600秒网格首次响应≤q构成事件；无事件仍为右删失。E=0的覆盖、缺少参考类别的比率、无共同事件的条件时间保持NA。五种子均值遇到任何未定义值不平均其余种子。有效零计数与无分母比率区分明确。
- 全七模型保留420/840行种子级CSV与84/168行汇总CSV，24行终值/J对比仅对应预定匹配集合−求和对比。该生成器不导出rho，也不赋予同火灾输入下fire-only微小浮点秩相关以几何意义。

独立实际接口检查只使用完整IID与ISO834：各35记录、70项阈值记录，合计140项计数、覆盖、平衡准确率与NA检查；IID覆盖存在正负参考类别的情形，ISO834的70项平衡准确率均为NA。各协议两项五种子标量差与显示缩放核对一致，元数据截断位置匹配；另核对这两个完整协议70项主预测回放记录。没有使用模拟数据，没有读取未完整三族成绩，也没有输出提前结果表。

审阅建议的两项校验补强已由作者合入并复核：平衡准确率按TP/E与TN/C重建，任一参考类缺失则必须NA；CPU回放的状态串与原始严格`<3e-6`规则一致，触发分支保留CPU差异并要求原GPU逐位精确。它们没有改变任何既有统计结果。

后续仍须等完整条件满足，再执行真实总表生成、逐行核对CSV/LaTeX数字及源SHA、编译`preview.tex`并检查实际页数、溢出和可读性；E=0或无共同事件的实际数据分支如出现，也应随真实输出复核。本次只写本目录审阅记录，未修改workflow、continuation、训练或主稿。
