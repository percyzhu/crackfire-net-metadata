# 应用流程与计算成本正文：独立增量核对

2026-09-07。**新增3.4、5.6及邻居实现措辞与现有研究范围、冻结计时方案一致，未发现需修改的实质问题。** 这是工作稿方法增量复核，不是完整稿件或投稿通过决定。

3.4把排序/阈值标记定位为拟用的归档代理优先级流程，说明高估响应会低估退化、延后标记或降低优先级，低估响应可能增加后续分析。它明确另需结构评估，不把相同权重处理不同裂缝数写成已验证的任意拓扑准确性；后续应结合完整家族结果保留实际误报、漏报和回穿限制。

5.6与计时接口一致：全部七模型seed42、160个固定IID案例、每例完整61点轨迹；CPU四线程、三轮轮换顺序、批量1/32，每个接口按批量在每轮先遍历一次完整人口再预热10批；CUDA在每次测量两端同步。驻留输入前向与共同输入至主机输出分开，后者包含特征、批整理、传输和推理，且所有模型承担相同公共特征成本。时间单位、吞吐量分母、批量摊销与单查询延迟区分准确；420个成功训练的主机时间与项目总耗时、独立架构效率、历史FE成本分开。正文明确计时延后执行，没有虚构延迟或加速倍数。

邻居规则使用versioned而非released，符合尚待公开发布的状态。已直接查看第8、14页PNG：新增段落完整可读，没有裁切、重叠或明显排版缺陷。主PDF SHA为 `fedbadb975dbcc23acedb3892587df5c805b45e0a9e989945fe5a8093205df60`，完整源与图面SHA保存在同名JSON。

本次没有运行计时、修改主稿或workflow/continuation。真实计时执行与结果核查、完整12协议结果整合及全稿审阅仍待完成。
