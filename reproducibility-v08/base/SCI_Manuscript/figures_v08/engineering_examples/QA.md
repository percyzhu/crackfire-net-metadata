# Preparation QA

Status: **PREPARED_GATE_TESTED_NO_FORMAL_FIGURE**.

The real `--render` command was exercised at 2026-09-07T06:14:53.596149+00:00 UTC. It stopped with exit code 2 because only five of ten fire-family protocols were complete and audited. External fire, linear, bilinear, log variant and smoldering were pending; 20 required engineering/review files were absent. See `gate_test.json` for the bound script hash and exact metadata paths.

The run opened zero prediction archives, zero family-performance files and zero review-performance documents. It created no formal figure, score table or source snapshot. It read only the reviewed selection plan, frozen training plan and global evaluation report. This verifies the actual missing-data guard, rather than a simulated successful plot.

Both Python programs passed syntax parsing. The generator is bound to the reviewed selection-plan SHA256 and unchanged fire-transfer validation-helper SHA256. The companion audit independently reconstructs selections, displayed curves, five-seed extrema and discrete threshold events from retained CSVs, and checks vector dimensions, minimum font size and source snapshot hashes.

**Still pending:** complete-data execution, independent numerical/export audit and visual inspection of the resulting real-data figure. No visual pass, model-performance result or selected example is claimed at this preparation stage. `audit_engineering_examples.py` deliberately leaves the eventual result at `PENDING_VISUAL_REVIEW` after its automated checks pass.

The response-axis range includes every raw reference, seed42 prediction and five-seed envelope extreme, with 4% padding beyond the encompassing data / unit interval. The eventual figure QA records these extrema and actual axis limits; the independent audit verifies that no raw response falls outside the limits. Values below zero or above one remain visible. This branch awaits real complete-data execution.

## Later complete-branch code review

See `complete_branch_code_review.md/.json` for the source/API review and two fixes. The latest actual gate test supersedes the earlier five-family readiness snapshot: seven families were complete at 2026-09-07T06:59:50.268050+00:00, and linear, bilinear and smoldering remained pending. No score/curve output or formal graphic was created. Frozen-input checks reproduced all 997 prescribed-fire histories exactly; the complete ISO NPZ was inspected only for headers and digest, never numerical prediction payload. Complete-data rendering and visual QA remain pending.
