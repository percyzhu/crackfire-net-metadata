\caption{Measured inference cost under a common engineering input interface. (a,b) Median (filled circle) and 95th-percentile (open diamond) single-query latency on CPU and CUDA; connecting segments describe observed timing quantiles, not confidence intervals. Each query returns the full 61-time response trajectory. (c,d) Batch-32 throughput, computed as total cases divided by total elapsed time, for resident-input model evaluation and the common interface from in-memory geometry and prescribed-fire parameters to host predictions. Measurements cover all 160 IID test geometries in three repeated rounds using the prespecified seed42 checkpoint of each model. The common interface constructs the same features for every model, including attributes that some baselines do not use; it is not an individually optimized deployment pipeline. CPU feature preparation, per-round results and crack-count strata are provided in the companion data. Loading, disk access and FE generation are excluded. No cross-hardware FE speedup is inferred.}
\label{fig:inference_efficiency}
